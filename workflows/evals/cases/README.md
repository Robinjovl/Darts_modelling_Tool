Case files hold configuration only (bed, workflow, prompt, budget, seeds, tolerance fractions,
gate names). Resolved targets live under `../truth/<case>/reference.json`, which is masked from
the evaluated agent. Example: `opt-place-proxy-smoke.json`.
